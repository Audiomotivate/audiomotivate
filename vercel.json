{
  "version": 2,
  "functions": {
    "server/index.ts": {
      "runtime": "@vercel/node@3"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/index.ts"
    },
    {
      "src": "/(.*)",
      "dest": "public/$1"
    }
  ],
  "buildCommand": "rm -f vite.config.ts && npx vite build --outDir public --root client && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=server"
}
